export interface IMindItem{
    text:string
}

export interface IData{
    originalText:string,
    originalTextArray: [string],
    translateText:string,
    translateTextArray : [string],
    fileBase64: string,
}